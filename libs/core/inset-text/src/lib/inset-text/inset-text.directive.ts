import { Directive } from '@angular/core';

/**
 * Base directive for inset text components across different design systems.
 * Provides common functionality that can be extended by specific design system implementations.
 */
@Directive()
export class InsetTextDirective {
  // Base directive with minimal implementation
  // Specific design system components will extend this
}
